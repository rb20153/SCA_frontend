# 前后端联调问题

本文档记录当前确认需要后端配合处理的问题。前端已根据现有接口实现可控的展示和跳转逻辑；以下问题不能通过前端伪造成功或绕过后端权限解决。

## 1. 检测工程师无法生成检测报告

### 页面与 API

- 页面：报告管理 / 报告列表
- 前端页面：[src/views/report/ReportList.vue](src/views/report/ReportList.vue)
- 前端组件：[src/components/report/ReportGenerateModal.vue](src/components/report/ReportGenerateModal.vue)
- 前端 API：[src/api/report.ts](src/api/report.ts) 中的 `generateReport`
- 请求：`POST /api/reports/generate`
- 请求体：`projectId`、`taskId`、`templateId`

### 问题描述

检测工程师点击“生成检测报告”后，接口返回 HTTP 403，无权限。

前端已经根据 `/reports` 页面 `write` 权限控制生成按钮显示。接口能够正常发出，403 表明后端已收到请求但拒绝了当前角色，前端不能通过修改按钮显示或请求代码解决权限拒绝。

### 后端解决建议

请确认并调整以下权限关系：

1. 检测工程师是否拥有 `/reports` 的写权限。
2. `/api/reports/generate` 是否错误要求了 `/reports/templates` 权限。
3. 生成报告权限是否应独立于报告模板的新增、编辑和发布权限。
4. 若后端返回独立的权限编码，请提供权限编码及角色授权规则，前端再同步按钮和路由权限判断。

### 联调验收

- 检测工程师可正常提交 `POST /api/reports/generate`。
- 报告模板管理权限不足时，不影响已经授权的报告生成权限。
- 未授权角色仍应由后端返回 403，前端仅负责隐藏入口并展示后端错误。

## 2. 检测任务完成或失败没有站内消息

### 页面与 API

- 任务页面：[src/views/detect](src/views/detect)
- 站内消息页面：[src/views/system/SiteMessage.vue](src/views/system/SiteMessage.vue)
- 站内消息查询 API：[src/api/siteMessage.ts](src/api/siteMessage.ts) 中的 `getSiteMessageList`
- 查询：`GET /api/system/messages`
- 未读数：`GET /api/system/messages/unread-count`

### 问题描述

自主率检测或开源风险检测完成、失败后，项目组成员没有收到站内消息通知。

当前前端只负责查询、展示和标记站内消息，没有在任务完成后创建消息的接口调用，也没有可靠的后台任务监听进程。用户离开页面后，前端无法继续承担通知生成职责。

### 后端解决建议

请在检测任务状态变为完成或失败时，由后端创建站内消息，并按项目组成员生成接收记录：

1. 接收人：任务所属项目组成员。
2. 消息类型：建议使用 `task`，前端会规范为任务通知。
3. 成功任务 action：`view_task_result`。
4. 失败任务 action：建议使用 `view_task_list`，或提供明确的失败日志跳转动作。
5. action 至少包含：`taskId`、`taskType`、`label`。
6. 消息内容包含任务名称、任务类型、最终状态和失败原因摘要。

### 联调验收

- 任务完成后，所有项目组成员均能在 `GET /api/system/messages` 查询到对应消息。
- 任务失败后，消息包含有效的 `taskId` 和 `taskType`。
- 点击成功消息进入对应结果页；点击失败消息进入对应任务或日志页面。
- 同一任务状态变更不会重复产生无意义的重复通知。

## 3. 报告完成或失败没有站内消息

### 页面与 API

- 报告页面：[src/views/report/ReportList.vue](src/views/report/ReportList.vue)
- 报告生成 API：[src/api/report.ts](src/api/report.ts) 中的 `generateReport`
- 请求：`POST /api/reports/generate`
- 站内消息查询 API：[src/api/siteMessage.ts](src/api/siteMessage.ts) 中的 `getSiteMessageList`
- 查询：`GET /api/system/messages`

### 问题描述

报告生成完成或失败后，发起报告生成的用户没有收到站内消息通知。

前端只能在当前页面提交报告生成请求并刷新列表，无法在用户离开页面后获知异步生成结果，也没有创建站内消息的接口。因此通知产生和接收人计算属于后端职责。

### 后端解决建议

请在报告异步任务完成或失败时，为报告生成发起人创建站内消息：

1. 接收人：提交 `POST /api/reports/generate` 的用户。
2. 消息类型：建议使用 `report`，前端会规范为报告通知。
3. action 至少包含：`reportId`、`label`。
4. 生成成功：使用 `view_report`，跳转报告列表并定位报告。
5. 生成失败：使用 `view_report` 或新增明确的失败原因动作，并携带 `reportId`。
6. 消息内容包含报告名称、生成结果和失败原因摘要。

### 联调验收

- 报告生成完成后，发起人能在 `GET /api/system/messages` 查询到成功通知。
- 报告生成失败后，发起人能查询到失败通知。
- 点击消息可以定位到对应报告；失败报告可以继续查看失败原因。
- 重复轮询或重复状态回调不会重复创建相同通知。

## 联调备注

- 第 5 项“告警详情证据和内容均为 JSON”本轮按要求跳过，未修改代码，也未纳入本文件的问题处理范围。
- 站内消息接口当前由前端负责读取和展示；任务、报告通知记录的创建、接收人计算和幂等控制需要后端完成。

## 4. 使用检测任务 TraceID 搜索日志仍无结果

### 页面与 API

- 页面：[src/views/system/LogList.vue](src/views/system/LogList.vue)
- 前端查询转换：[src/utils/logQuery.ts](src/utils/logQuery.ts)
- 前端 API：[src/api/system.ts](src/api/system.ts) 中的 `getLogList`
- 请求：`GET /api/system/logs`
- 查询参数：`traceId`、分页参数及其他日志筛选条件

### 问题描述

从失败检测任务列表中取得后端返回的 `traceId`，在日志列表页面手动输入该 TraceID 搜索，仍然没有返回日志记录。

当前前端已经使用 `traceId` 作为失败任务跳转和日志列表查询条件。若手动输入同一个 TraceID 也无结果，问题不能仅通过前端更换 URL 参数解决，需要后端核对日志数据和查询实现。

### 后端解决建议

请重点核对以下内容：

1. 检测任务返回的 `traceId` 是否就是审计日志表中的 `trace_id`，是否存在大小写、前后空格或格式转换。
2. 检测任务失败时是否实际写入了审计日志，而不是只在任务表保存 TraceID。
3. `GET /api/system/logs` 是否支持 `traceId` 查询参数，参数名称是否实际为 `trace_id`、`traceID` 或其他名称。
4. 日志接口是否把 `traceId` 误当作模糊查询、分页条件或其他字段处理。
5. 返回的日志记录是否包含与任务一致的 `traceId`，而不是仅在详情或内部字段中保存。
6. 请提供一组可核对的任务 `traceId`、日志记录 `traceId` 和实际请求示例，便于前后端确认数据链路。

### 联调验收

- 使用任务列表返回的 TraceID 调用 `GET /api/system/logs?traceId=xxx` 能返回对应日志。
- 页面手动输入同一个 TraceID 与 URL 直传查询结果一致。
- 失败任务点击“查看日志”后能自动带入 TraceID 并展示对应日志。
