# 季度更新管理新增接口说明

## 统一约定

- 模块：知识库 - 季度更新管理（`/knowledge/quarter-updates`）
- 请求头：`Authorization: Bearer <token>`
- 请求体：`application/json`
- 成功响应：HTTP 2xx 且 `code` 为 `0` 或兼容值 `200`
- 失败响应：前端展示响应中的 `message`

成功响应示例：

```json
{
  "code": 0,
  "message": "操作成功",
  "data": null
}
```

## 1. 开始季度更新

- 前端函数：`startKbQuarterUpdate`
- HTTP 方法：`POST`
- 路径：`/api/knowledge/quarter-updates/start`
- 场景：用户在“开始季度更新”确认弹窗中选择“是”后调用。
- 请求体：无
- 成功后前端行为：提示“操作成功”，刷新季度更新概览和记录列表。

## 2. 季度回滚

- 前端函数：`rollbackKbQuarterUpdate`
- HTTP 方法：`POST`
- 路径：`/api/knowledge/quarter-updates/rollback`
- 场景：用户选择回滚季度并点击“确定”后调用。

请求体：

```json
{
  "quarter": "2026 Q3"
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `quarter` | string | 是 | 目标历史季度，取值来自季度选项接口。 |

成功后前端行为：提示“操作成功”，刷新季度更新概览和记录列表。

## 3. 已有季度选项接口

- 前端函数：`getKbQuarterUpdateQuarterOptions`
- HTTP 方法：`GET`
- 路径：`/api/knowledge/quarter-updates/quarters`
- 场景：每次打开“季度回滚”弹窗时调用，用于加载下拉选项。

成功响应示例：

```json
{
  "code": 0,
  "message": "操作成功",
  "data": ["2026 Q3"]
}
```
